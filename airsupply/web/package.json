{
  "name": "airsupply-web",
  "version": "0.0.0",
  "private": true,
  "description": "The airsupply page. Elm all the way down: state, view, and the HTTP to the add-on's API. npm is here for the elm compiler and nothing else.",
  "scripts": {
    "build": "elm make src/Main.elm --optimize --output=dist/assets/elm.js && cp index.html dist/",
    "dev": "elm make src/Main.elm --output=dist/assets/elm.js && cp index.html dist/"
  },
  "devDependencies": {
    "elm": "0.19.2-0"
  }
}
